# Sistema de sonido y pantalla de Créditos — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que el juego suene: música por pantalla y nivel con fundido cruzado y capa de tensión,
efectos de partida (algunos posicionales respecto de la moto), motor, horda, ambiente y UI, tres
volúmenes en Opciones, y una pantalla de Créditos nueva. Todo con placeholders reemplazables.

**Architecture:** La lógica es C# plano y testeada (`Scripts/Audio/Core`): buses, curva de
volumen, elección de variantes, paneo/atenuación, regla de transición de música, tono del motor,
resolución de bancos. Los MonoBehaviours solo la aplican: `AudioDirector` + `MusicDirector` viven
en `Boot` (nunca se descarga), `SfxDirector`/`MotorSonido`/`HordaSonido` en cada nivel, y
`UiSonidos`/`SonidosAleatorios` en las pantallas. Los datos son ScriptableObjects editables.

**Tech Stack:** Unity 6000.6, `AudioSource`/`AudioClip`, uGUI + Input System, NUnit EditMode.

Spec: `docs/superpowers/specs/2026-09-21-sonido-design.md` (léase entero antes de cada tarea de
integración; este plan no repite cada número).

## Global Constraints

- Proyecto Unity `Juego/` (nunca la raíz), rama `Jose`. Editor abierto; se maneja por MCP
  (ver `.git/sdd/unity-context.md`).
- `Juego/Assets/_Zombineta/Simulacion` es compartida con `Prototipo/`: solo cambios aditivos
  (enum al final, campos nuevos, métodos nuevos). No cambiar reglas del juego.
- Nunca commitear `Juego/ProjectSettings/ProjectSettings.asset`.
- **No usar `AudioMixer`.** Mezcla por buses de código (`AudioBuses`). Ningún script asigna
  `AudioSource.volume` salvo el componente que aplica el bus.
- `AudioListener.volume` queda en 1.
- Un componente que mueve su transform cada cuadro nunca va en el objeto `Nivel` (trampa de
  `HordeGlow`): va en un objeto propio sin hijos.
- Números del spec, textuales: volúmenes default 0,8; claves `zombineta.musicVolume`,
  `zombineta.sfxVolume`; curva `v²`; pausa: Efectos/Ambiente → 0 en 0,2 s reales, Música ×0,4;
  fundido cruzado 1,5 s; tensión 0 a ≥45 m, 1 encima, suavizado 0,5 s; espacial `anchoPaneo`
  12 m, `distanciaPlena` 4 m, `distanciaMaxima` 30 m; motor 0,8 / 1,2 / 1,45, suavizado 0,15 s,
  sin nafta a 0 en 1 s, al perder 0,3 s; ambiente entra en 1 s; pool de 16 fuentes; Créditos
  Submit ×3.
- Nombres de archivo `tipo_nombre_variante` bajo `Juego/Assets/_Zombineta/Audio/{Musica,Sfx,Ambiente}/`.
- Verificación en Play: callback registrado antes de entrar en Play, resultados a `SessionState`,
  entrar por el menú (`Boot` → `GameRoot.Flow.Play()` → esperar ~120 frames entre pasos). Se
  verifica leyendo el estado de las `AudioSource`, no escuchando. No enfocar ventanas ni leer
  píxeles de pantalla.
- Commits terminan con `Co-Authored-By:` del modelo que los escribió.

## Modelos por tarea

| Tarea | Implementa | Revisa |
|---|---|---|
| 1. Núcleo puro (buses, curva, variantes, espacial, música, motor, banco) | haiku | sonnet |
| 2. Flujo y pantalla de Créditos | sonnet | sonnet |
| 3. Datos de audio (ScriptableObjects) y volúmenes en Opciones | sonnet | sonnet |
| 4. `AudioDirector` en Boot: pool, buses aplicados, UI y aleatorios | sonnet | sonnet |
| 5. `MusicDirector`: temas, fundido cruzado y tensión | sonnet | sonnet |
| 6. Sonido del nivel: `SfxDirector`, motor, horda, ambiente | sonnet | sonnet |
| 7. Placeholders, assets y cableado en escenas | sonnet | sonnet |
| 8. Verificación en Play, guía de sonido y HANDOFF | sonnet | — |
| Revisión final de la rama | — | opus |

---

### Task 1: Núcleo puro

**Files (todo nuevo):**
- `Juego/Assets/_Zombineta/Scripts/Audio/Core/AudioBuses.cs`
- `Juego/Assets/_Zombineta/Scripts/Audio/Core/VariantPicker.cs`
- `Juego/Assets/_Zombineta/Scripts/Audio/Core/SonidoEspacial.cs`
- `Juego/Assets/_Zombineta/Scripts/Audio/Core/MusicaRegla.cs`
- `Juego/Assets/_Zombineta/Scripts/Audio/Core/MotorTono.cs`
- `Juego/Assets/_Zombineta/Scripts/Audio/Core/SonidoClave.cs`
- Test: `Juego/Assets/_Zombineta/Tests/Editor/AudioCoreTests.cs`

(`Scripts/` es el asmdef `Zombineta.Juego`; los tests van en `Zombineta.Juego.Tests`, que ya lo
referencia.)

**Interfaces (Produces):**
- `enum AudioBus { Musica, Efectos, Ambiente, UI }`
- `sealed class AudioBuses` — `float General, Musica, Efectos` (0–1, lineales de los sliders);
  `float PausaDuck` (0–1, 1 = sin pausa); `float Gain(AudioBus bus)`; `static float Curva(float v)`.
- `sealed class VariantPicker` — `VariantPicker(int seed)`; `int Pick(int count)`.
- `struct EspacialConfig { float anchoPaneo, distanciaPlena, distanciaMaxima; }` y
  `static class SonidoEspacial` — `float Pan(float dx, EspacialConfig c)`, `float Volumen(float dx, EspacialConfig c)`.
- `enum MusicaAccion { Seguir, Cruzar, Silenciar }` y `static class MusicaRegla` —
  `MusicaAccion Decidir(object actual, object nuevo)`.
- `static class MotorTono` — `float Objetivo(float velocidad, float velocidadNormal, bool turbo)`.
- `enum SonidoClave` (todas las claves del spec, en ese orden).

- [ ] **Step 1: Write the failing tests** — `AudioCoreTests.cs`:

```csharp
using NUnit.Framework;
using Zombineta.Audio;

namespace Zombineta.Juego.Tests
{
    public class AudioCoreTests
    {
        // --- Buses ---
        [Test] public void CurvaEsCuadratica()
        {
            Assert.AreEqual(0f, AudioBuses.Curva(0f), 1e-5f);
            Assert.AreEqual(0.25f, AudioBuses.Curva(0.5f), 1e-5f);
            Assert.AreEqual(1f, AudioBuses.Curva(1f), 1e-5f);
            Assert.AreEqual(1f, AudioBuses.Curva(2f), 1e-5f);
            Assert.AreEqual(0f, AudioBuses.Curva(-1f), 1e-5f);
        }

        [Test] public void GananciaMultiplicaBusYGeneral()
        {
            var b = new AudioBuses { General = 1f, Musica = 0.5f, Efectos = 1f, PausaDuck = 1f };
            Assert.AreEqual(0.25f, b.Gain(AudioBus.Musica), 1e-5f);
            b.General = 0.5f;
            Assert.AreEqual(0.0625f, b.Gain(AudioBus.Musica), 1e-5f);
        }

        [Test] public void UiYAmbienteCuelganDeEfectos()
        {
            var b = new AudioBuses { General = 1f, Musica = 1f, Efectos = 0.5f, PausaDuck = 1f };
            Assert.AreEqual(0.25f, b.Gain(AudioBus.UI), 1e-5f);
            Assert.AreEqual(0.25f, b.Gain(AudioBus.Ambiente), 1e-5f);
        }

        [Test] public void PausaCallaEfectosYAmbientePeroNoUi()
        {
            var b = new AudioBuses { General = 1f, Musica = 1f, Efectos = 1f, PausaDuck = 0f };
            Assert.AreEqual(0f, b.Gain(AudioBus.Efectos), 1e-5f);
            Assert.AreEqual(0f, b.Gain(AudioBus.Ambiente), 1e-5f);
            Assert.AreEqual(1f, b.Gain(AudioBus.UI), 1e-5f);
        }

        [Test] public void PausaBajaMusicaA40PorCiento()
        {
            var b = new AudioBuses { General = 1f, Musica = 1f, Efectos = 1f, PausaDuck = 0f };
            Assert.AreEqual(0.4f, b.Gain(AudioBus.Musica), 1e-5f);
        }

        // --- Variantes ---
        [Test] public void VarianteNuncaRepiteLaAnterior()
        {
            var p = new VariantPicker(7);
            int last = p.Pick(3);
            for (int i = 0; i < 200; i++)
            {
                int next = p.Pick(3);
                Assert.AreNotEqual(last, next);
                Assert.That(next, Is.InRange(0, 2));
                last = next;
            }
        }

        [Test] public void VarianteConUnaSolaOpcionEsCero()
        {
            var p = new VariantPicker(1);
            Assert.AreEqual(0, p.Pick(1));
            Assert.AreEqual(0, p.Pick(1));
            Assert.AreEqual(-1, p.Pick(0));
        }

        [Test] public void VarianteEsDeterministaConLaMismaSemilla()
        {
            var a = new VariantPicker(42);
            var b = new VariantPicker(42);
            for (int i = 0; i < 20; i++)
                Assert.AreEqual(a.Pick(5), b.Pick(5));
        }

        // --- Espacial ---
        static readonly EspacialConfig C = new EspacialConfig { anchoPaneo = 12f, distanciaPlena = 4f, distanciaMaxima = 30f };

        [Test] public void PaneoAtrasIzquierdaAdelanteDerecha()
        {
            Assert.AreEqual(-0.5f, SonidoEspacial.Pan(-6f, C), 1e-5f);
            Assert.AreEqual(0.5f, SonidoEspacial.Pan(6f, C), 1e-5f);
            Assert.AreEqual(-1f, SonidoEspacial.Pan(-50f, C), 1e-5f);
            Assert.AreEqual(1f, SonidoEspacial.Pan(50f, C), 1e-5f);
            Assert.AreEqual(0f, SonidoEspacial.Pan(0f, C), 1e-5f);
        }

        [Test] public void VolumenPlenoCercaYCeroLejos()
        {
            Assert.AreEqual(1f, SonidoEspacial.Volumen(0f, C), 1e-5f);
            Assert.AreEqual(1f, SonidoEspacial.Volumen(-4f, C), 1e-5f);
            Assert.AreEqual(0.5f, SonidoEspacial.Volumen(17f, C), 1e-5f);
            Assert.AreEqual(0f, SonidoEspacial.Volumen(-30f, C), 1e-5f);
            Assert.AreEqual(0f, SonidoEspacial.Volumen(99f, C), 1e-5f);
        }

        // --- Música ---
        [Test] public void MismoTemaSigue()
        {
            var tema = new object();
            Assert.AreEqual(MusicaAccion.Seguir, MusicaRegla.Decidir(tema, tema));
        }

        [Test] public void SinTemaNuevoSigueElQueVenia()
        {
            Assert.AreEqual(MusicaAccion.Seguir, MusicaRegla.Decidir(new object(), null));
            Assert.AreEqual(MusicaAccion.Seguir, MusicaRegla.Decidir(null, null));
        }

        [Test] public void TemaDistintoCruza()
        {
            Assert.AreEqual(MusicaAccion.Cruzar, MusicaRegla.Decidir(new object(), new object()));
            Assert.AreEqual(MusicaAccion.Cruzar, MusicaRegla.Decidir(null, new object()));
        }

        // --- Motor ---
        [Test] public void TonoDelMotor()
        {
            Assert.AreEqual(0.8f, MotorTono.Objetivo(0f, 12f, false), 1e-5f);
            Assert.AreEqual(1.2f, MotorTono.Objetivo(12f, 12f, false), 1e-5f);
            Assert.AreEqual(1.0f, MotorTono.Objetivo(6f, 12f, false), 1e-5f);
            Assert.AreEqual(1.45f, MotorTono.Objetivo(12f, 12f, true), 1e-5f);
            Assert.AreEqual(0.8f, MotorTono.Objetivo(-6f, 12f, false), 1e-5f);
            Assert.AreEqual(1.2f, MotorTono.Objetivo(30f, 12f, false), 1e-5f);
        }
    }
}
```

`MusicaAccion.Silenciar` no la produce `Decidir`: la usa el director al salir del nivel para la
capa de tensión (se deja en el enum para que Task 5 no toque este archivo).

- [ ] **Step 2: Run the tests and see them fail** (no compilan: los tipos no existen).

- [ ] **Step 3: Implement.** Todo en `namespace Zombineta.Audio`.

```csharp
// AudioBuses.cs
using UnityEngine;

namespace Zombineta.Audio
{
    public enum AudioBus { Musica, Efectos, Ambiente, UI }

    /// <summary>
    /// La mezcla del juego sin AudioMixer (Unity no deja crearlo por script). Los sliders
    /// guardan valores lineales 0-1; la ganancia usa una curva perceptual. UI y Ambiente
    /// cuelgan de Efectos. La pausa (PausaDuck 0) calla Efectos y Ambiente y deja la musica
    /// al 40 %; UI no se toca.
    /// </summary>
    public sealed class AudioBuses
    {
        public const float MusicaEnPausa = 0.4f;

        public float General = 1f;
        public float Musica = 1f;
        public float Efectos = 1f;
        public float PausaDuck = 1f;

        public static float Curva(float v)
        {
            v = Mathf.Clamp01(v);
            return v * v;
        }

        public float Gain(AudioBus bus)
        {
            float general = Curva(General);
            float duck = Mathf.Clamp01(PausaDuck);
            switch (bus)
            {
                case AudioBus.Musica:
                    return general * Curva(Musica) * Mathf.Lerp(MusicaEnPausa, 1f, duck);
                case AudioBus.UI:
                    return general * Curva(Efectos);
                default: // Efectos, Ambiente
                    return general * Curva(Efectos) * duck;
            }
        }
    }
}
```

```csharp
// VariantPicker.cs
namespace Zombineta.Audio
{
    /// <summary>Elige una variante al azar sin repetir la ultima. Semilla inyectable para tests.</summary>
    public sealed class VariantPicker
    {
        readonly System.Random rng;
        int last = -1;

        public VariantPicker(int seed) => rng = new System.Random(seed);

        public int Pick(int count)
        {
            if (count <= 0)
                return -1;
            if (count == 1)
                return last = 0;
            int pick = rng.Next(count - 1);
            if (last >= 0 && pick >= last)
                pick++;
            return last = pick;
        }
    }
}
```

```csharp
// SonidoEspacial.cs
using System;
using UnityEngine;

namespace Zombineta.Audio
{
    [Serializable]
    public struct EspacialConfig
    {
        public float anchoPaneo;
        public float distanciaPlena;
        public float distanciaMaxima;
    }

    /// <summary>Paneo y atenuacion de un sonido segun su distancia en X a la moto (dx = sonido - moto).</summary>
    public static class SonidoEspacial
    {
        public static float Pan(float dx, EspacialConfig c) =>
            c.anchoPaneo <= 0f ? 0f : Mathf.Clamp(dx / c.anchoPaneo, -1f, 1f);

        public static float Volumen(float dx, EspacialConfig c)
        {
            float d = Mathf.Abs(dx);
            if (d <= c.distanciaPlena)
                return 1f;
            if (d >= c.distanciaMaxima || c.distanciaMaxima <= c.distanciaPlena)
                return 0f;
            return 1f - (d - c.distanciaPlena) / (c.distanciaMaxima - c.distanciaPlena);
        }
    }
}
```

```csharp
// MusicaRegla.cs
namespace Zombineta.Audio
{
    public enum MusicaAccion { Seguir, Cruzar, Silenciar }

    /// <summary>Que hace la musica ante un cambio de pantalla. Sin tema nuevo, sigue el que venia.</summary>
    public static class MusicaRegla
    {
        public static MusicaAccion Decidir(object actual, object nuevo)
        {
            if (nuevo == null || ReferenceEquals(actual, nuevo))
                return MusicaAccion.Seguir;
            return MusicaAccion.Cruzar;
        }
    }
}
```

Note: callers pass `AudioClip`s. Unity's `==` override doesn't matter here because the director
passes real (non-destroyed) clips or `null`; `ReferenceEquals` on two references to the same
asset is true.

```csharp
// MotorTono.cs
using UnityEngine;

namespace Zombineta.Audio
{
    /// <summary>Tono objetivo del loop del motor: 0,8 quieta, 1,2 a velocidad normal, 1,45 con turbo.</summary>
    public static class MotorTono
    {
        public const float Quieta = 0.8f;
        public const float Normal = 1.2f;
        public const float Turbo = 1.45f;

        public static float Objetivo(float velocidad, float velocidadNormal, bool turbo)
        {
            if (turbo)
                return Turbo;
            float t = velocidadNormal <= 0f ? 0f : Mathf.Clamp01(velocidad / velocidadNormal);
            return Mathf.Lerp(Quieta, Normal, t);
        }
    }
}
```

```csharp
// SonidoClave.cs
namespace Zombineta.Audio
{
    /// <summary>Cada sonido que el juego sabe pedir. Agregar al final: los bancos lo guardan como int.</summary>
    public enum SonidoClave
    {
        Disparo, SinBalas, CambioCarril, Choque, Atropello, ExplosionBarril,
        PickupNafta, PickupBateria, PickupMunicion, FaroOn, FaroOff, SinNafta,
        Salto, Aterrizaje, AterrizajePerfecto, Victoria, Derrota,
        Motor, Horda, ZombieAdelante,
        UiMover, UiConfirmar, UiVolver,
    }
}
```

(`ZombieAdelante` es el gemido de los zombies sueltos del recorrido, del spec §4.)

- [ ] **Step 4: Run the full EditMode suite** — 243 + 14 = 257 en verde.

- [ ] **Step 5: Commit** `Audio: nucleo puro (buses, variantes, espacial, musica, motor)`.

---

### Task 2: Flujo y pantalla de Créditos

**Files:**
- Modify: `Juego/Assets/_Zombineta/Simulacion/Runtime/Flow/GameFlow.cs`
- Modify: `Juego/Assets/_Zombineta/Simulacion/Tests/Editor/GameFlowTests.cs`
- Modify: `Juego/Assets/_Zombineta/Scripts/Flow/SceneRoutePlanner.cs` (`SceneNames.Credits`, `ScreenScene`)
- Modify: `Juego/Assets/_Zombineta/Scripts/Flow/GameRoot.cs` (`TryScreenOf` conoce `Credits`)
- Create: `Juego/Assets/_Zombineta/Scripts/Screens/CreditsScreen.cs`, `Scripts/Screens/Creditos.cs` (ScriptableObject)
- Modify: `Scripts/Screens/MainMenuScreen.cs` (`OpenCredits`), `Scripts/Screens/EndingScreen.cs` (`ShowCredits`)
- Create: `Scenes/Credits.unity`, `Settings/Creditos.asset`; Modify: Build Settings, `MainMenu.unity`, `Ending.unity`

**Interfaces (Produces):** `GameScreen.Credits` (último valor del enum);
`bool GameFlow.OpenCredits()` (solo desde `MainMenu`); `bool GameFlow.ShowCredits()` (solo desde
`Ending`); `GameFlow.ToMainMenu()` también acepta `Credits`; `SceneNames.Credits = "Credits"`.

- [ ] **Step 1: Failing tests** en `GameFlowTests.cs` (mismo estilo que los existentes):
  `OpenCreditsDesdeElMenu` (MainMenu → Credits, true); `OpenCreditsFueraDelMenuNoHaceNada`
  (desde Playing/Ending devuelve false y no cambia); `ShowCreditsDesdeElEnding` (llegar a Ending
  ganando el último nivel, `ShowCredits()` → Credits); `ShowCreditsFueraDelEndingNoHaceNada`;
  `DeCreditosAlMenu` (`ToMainMenu()` desde Credits → MainMenu); `CreditsEsElUltimoDelEnum`
  (`(int)GameScreen.Credits == Enum.GetValues(typeof(GameScreen)).Length - 1`). Y en los tests del
  planner (`Juego/Assets/_Zombineta/Tests/Editor`, donde estén los de `SceneRoutePlanner`):
  `Credits` es una base (`ScreenScene(Credits) == "Credits"`, ir desde el menú descarga el menú y
  carga Credits con fundido).
- [ ] **Step 2:** verlos fallar.
- [ ] **Step 3: Implement** (aditivo): `Credits` al final del enum; los dos métodos con
  `Go(from, to)`; `ToMainMenu` suma `Current == GameScreen.Credits`; actualizar el comentario del
  diagrama (`Menu -> Creditos -> Menu`, `Final -> Creditos -> Menu`). `SceneNames.Credits`,
  `ScreenScene`, `TryScreenOf`.
- [ ] **Step 4: `Creditos` (ScriptableObject)**: `[Serializable] class Seccion { string titulo; string[] lineas; }`,
  `List<Seccion> secciones`, `float velocidad = 60f` (px/s), `float pausaFinal = 2f`.
  `Settings/Creditos.asset` con secciones placeholder: "Zombineta"; "Equipo" (Germán, Jesús,
  José, Juana, Seba); "Taller de Proyecto Integral 212"; "Gracias por jugar".
- [ ] **Step 5: `CreditsScreen : ScreenBase`**: arma un `Text` por línea dentro de un contenedor
  que sube a `velocidad` (×3 mientras `submitAction` está apretado), en tiempo real
  (`unscaledDeltaTime`); cuando la última línea pasa el borde de arriba, espera `pausaFinal` y
  llama `Flow.ToMainMenu()`; `cancelAction` o el botón "Volver" también. Respeta
  `InputConsumed` y `TopScene` como `OptionsScreen`.
- [ ] **Step 6: Escenas.** Construir `Credits.unity` siguiendo el estilo de las pantallas que
  arma `Editor/SkeletonSceneBuilder.cs` (canvas, fondo, `EventSystem` no — vive en Boot, botón
  "Volver" como `firstSelected`); agregarla a Build Settings después de `Ending`. En `MainMenu`,
  botón "Créditos" entre Opciones y Salir, cableado a `MainMenuScreen.OpenCredits` y con la
  navegación de los botones rehecha. En `Ending`, el botón pasa a "Continuar" →
  `EndingScreen.ShowCredits` (se quita el "Menú principal"). Usar el editor/RunCommand para las
  escenas; no editar YAML a mano.
- [ ] **Step 7:** suite completa verde. Play desde el menú: `OpenCredits()` carga `Credits`, el
  texto sube y a los pocos segundos (con `velocidad` alta temporal para el test) vuelve al menú;
  desde `Ending` (`JumpTo(Ending)` en Play desde `Ending.unity`) `ShowCredits()` carga Credits.
- [ ] **Step 8: Commit** `Creditos: pantalla nueva desde el menu y despues del final`.

---

### Task 3: Datos de audio y volúmenes

**Files:**
- Create en `Scripts/Audio/Data/`: `Sonido.cs`, `BancoDeSonidos.cs`, `AudioDeNivel.cs`, `MusicaDelJuego.cs`, `EspacialAsset.cs`
- Modify: `Simulacion/Runtime/Core/GameSettings.cs` (`MusicVolume`, `SfxVolume`)
- Modify: `Simulacion/Runtime/Flow/LevelSequence.cs` (`LevelInfo.audio`) — **ver nota**
- Modify: `Scripts/Screens/OptionsScreen.cs`, `Scenes/Options.unity` (tres sliders)
- Modify: `Scripts/Flow/GameRoot.cs` (`ApplySettings`)
- Test: `Tests/Editor/AudioDataTests.cs`

**Nota sobre `LevelInfo.audio`:** `LevelInfo` vive en la simulación (asmdef `Zombineta.Simulacion`),
que no ve `Zombineta.Juego`. El campo se declara como `ScriptableObject audio` (con tooltip "un
AudioDeNivel"), y el juego lo castea. Es aditivo y no arrastra dependencias.

**Interfaces (Produces):**
- `Sonido : ScriptableObject` — `AudioClip[] clips`; `Vector2 volumen = (0.9, 1)`;
  `Vector2 tono = (0.95, 1.05)`; `AudioBus bus = Efectos`; `bool posicional`; `bool loop`;
  `float cooldown = 0.05f`.
- `BancoDeSonidos : ScriptableObject` — `[Serializable] struct Entrada { SonidoClave clave; Sonido sonido; }`,
  `List<Entrada> entradas`; `Sonido Buscar(SonidoClave k)`; `static Sonido Resolver(BancoDeSonidos nivel, BancoDeSonidos global, SonidoClave k)`
  (nivel primero, si no global, si no null).
- `AudioDeNivel : ScriptableObject` — `AudioClip musica`, `AudioClip tension`, `Sonido ambiente`, `BancoDeSonidos banco`.
- `MusicaDelJuego : ScriptableObject` — un `AudioClip` por pantalla (`menu, opciones, personaje,
  cinematica, nivelCompleto, gameOver, ending, creditos`); `float fundido = 1.5f`;
  `AudioClip Para(GameScreen s)` (Playing y Paused devuelven null: los maneja el director).
- `EspacialAsset : ScriptableObject` — `EspacialConfig config = {12, 4, 30}`.
- `GameSettings.MusicVolume`, `GameSettings.SfxVolume` (0–1, default 0.8, claves del spec,
  mismo patrón que `Volume`) y `static event Action AudioSettingsChanged` que disparan los tres
  setters de volumen.

- [ ] **Step 1: Failing tests** `AudioDataTests.cs`: `Resolver` (nivel pisa global; clave que
  el nivel no tiene cae al global; ninguno → null; banco nivel null → global);
  `MusicaDelJuego.Para` (cada pantalla devuelve su campo; Playing/Paused → null); volúmenes
  (`MusicVolume`/`SfxVolume` default 0,8 tras `PlayerPrefs.DeleteKey`, se clampéan, y restaurar
  el valor previo en `TearDown`).
- [ ] **Step 2:** verlos fallar. **Step 3:** implementar.
- [ ] **Step 4: Opciones:** `OptionsScreen` pasa de un `volume` a `volumeGeneral`,
  `volumeMusica`, `volumeEfectos` (renombrar el campo existente con `[FormerlySerializedAs("volume")]`
  a `volumeGeneral`), cada uno escribe su setting y llama `GameRoot.Instance?.ApplySettings()`.
  En `Options.unity`, duplicar la fila del slider existente dos veces con etiquetas "Música" y
  "Efectos" (la actual pasa a "Volumen general"), rehacer la navegación y reacomodar las filas
  para que no se encimen; verificar con una captura.
- [ ] **Step 5: `GameRoot.ApplySettings`:** `AudioListener.volume = 1f;` y
  `AudioDirector.Instance?.AplicarVolumenes();` (el director llega en Task 4: por ahora dejar la
  llamada a través de `GameSettings.AudioSettingsChanged`, que el director escuchará, y no
  referenciar tipos que no existen).
- [ ] **Step 6:** suite verde. **Step 7: Commit** `Audio: datos (Sonido, bancos, musica) y tres volumenes`.

---

### Task 4: `AudioDirector` en Boot

**Files:**
- Create: `Scripts/Audio/AudioDirector.cs`, `Scripts/Audio/FuenteConBus.cs`, `Scripts/Audio/UiSonidos.cs`, `Scripts/Audio/SonidosAleatorios.cs`
- Modify: `Scenes/Boot.unity` (objeto `Audio` con `AudioDirector`)

**Interfaces (Produces):**
- `FuenteConBus : MonoBehaviour` — requiere `AudioSource`; `AudioBus bus`; `float volumenPropio`;
  `float espacial = 1f` (lo escribe quien la maneje); en `LateUpdate`:
  `source.volume = volumenPropio * espacial * AudioDirector.Buses.Gain(bus)`. Es el **único**
  lugar que escribe `AudioSource.volume`.
- `AudioDirector : MonoBehaviour` (Boot, `[DefaultExecutionOrder(-900)]`) —
  `static AudioDirector Instance`; `static AudioBuses Buses` (nunca null: si no hay director, un
  `AudioBuses` por defecto); `BancoDeSonidos global` (serializado); `EspacialAsset espacial`;
  `BancoDeSonidos BancoNivel { get; set; }` (lo pone el nivel);
  `void Play(SonidoClave k, float? worldX = null)` y `void Play(Sonido s, float? worldX = null)`;
  `float PlayerWorldX { get; set; }` (lo actualiza el nivel; para el espacial);
  `void AplicarVolumenes()`; `EspacialConfig Espacial`.
- `UiSonidos` (en cada pantalla con menú), `SonidosAleatorios` (Game Over, Ending, Créditos).

Comportamiento (spec §1, §3, §4):
- Pool de 16 hijos con `AudioSource` + `FuenteConBus` (`playOnAwake = false`, `spatialBlend = 0`).
  `Play` resuelve con `BancoDeSonidos.Resolver(BancoNivel, global, k)`; respeta `cooldown` por
  `Sonido` (tiempo real); elige clip con un `VariantPicker` por `Sonido` (diccionario, semilla
  `sonido.GetInstanceID()`); volumen y tono al azar dentro de los rangos; si `posicional` y hay
  `worldX`, `panStereo = Pan(dx)` y `espacial = Volumen(dx)` con `dx` en **metros**
  (`(worldX - PlayerWorldX) / worldUnitsPerMeter` — el nivel le pasa el factor al registrarse, ver
  Task 6) y si `Volumen` da 0 no suena. Pool lleno: pisa el más viejo que no sea loop. `Play` de
  un `Sonido` con `loop` se rechaza con un warning (los loops tienen su propio componente).
- `AplicarVolumenes()` copia `GameSettings.Volume/MusicVolume/SfxVolume` a `Buses`; se llama en
  `Awake` y cuando dispara `GameSettings.AudioSettingsChanged`.
- Pausa: escucha `GameRoot.Flow.Changed`; `PausaDuck` va a 0 si la pantalla actual es `Paused`
  (u Opciones abierta desde la pausa: usar `GameRoot.Instance.TopScene`/overlays — lo que el
  director de escenas ya expone) y a 1 si no, con un `MoveTowards` en `unscaledDeltaTime` a
  razón de 1/0,2 s.
- `UiSonidos`: en `Update`, si esta escena es `TopScene`, compara
  `EventSystem.current.currentSelectedGameObject` con el anterior (dentro de esta escena) →
  `UiMover` (no en el primer frame en que se selecciona solo); `submit`/`cancel`
  `InputActionReference` `WasPressedThisFrame` → `UiConfirmar`/`UiVolver`. Respeta
  `ScreenBase.InputConsumed`-equivalente (`Time.frameCount == GameRoot.Instance.LastChangeFrame`
  no suena, para no duplicar con la pantalla anterior).
- `SonidosAleatorios`: `Sonido[] sonidos`, `Vector2 intervalo = (6, 14)` segundos reales; arranca
  esperando un intervalo al azar; nunca dos a la vez.
- Tests EditMode: no hacen falta más allá de Task 1 (la lógica está ahí); verificar en Play.
- [ ] **Steps:** implementar, poner el objeto en `Boot.unity` (con `global` y `espacial` vacíos por
  ahora: Task 7 los crea), `UiSonidos` en MainMenu, Options, CharacterSelect, LevelComplete,
  GameOver, Ending, Credits y Pause (con las `InputActionReference` UI/Submit y UI/Cancel que ya
  usan esas pantallas); `SonidosAleatorios` en GameOver, Ending y Credits (lista vacía).
  Compilar limpio, suite verde. Play desde el menú: `AudioDirector.Instance` existe, 16 fuentes,
  `Buses` refleja los settings, al pausar `PausaDuck` baja a 0 y al reanudar vuelve a 1.
  **Commit** `Audio: director en Boot, pool, buses, UI y aleatorios`.

---

### Task 5: `MusicDirector`

**Files:** Create `Scripts/Audio/MusicDirector.cs`; Modify `Scenes/Boot.unity` (mismo objeto `Audio`).

**Interfaces:** Consume `MusicaDelJuego`, `AudioDeNivel` (vía `GameRoot.Instance.CurrentLevel.audio as AudioDeNivel`),
`MusicaRegla.Decidir`, `FuenteConBus`. Produce `static MusicDirector Instance`,
`void SetAmenaza(float amenaza01)` (lo llama el nivel, Task 6), y para verificación
`AudioSource Actual`, `AudioSource Tension`.

Comportamiento (spec §2):
- Dos fuentes de tema (A/B, bus Musica, loop) + una de tensión (bus Musica, loop). Todas con
  `FuenteConBus`; el fundido se hace sobre `volumenPropio`, en tiempo real.
- En `GameRoot.Flow.Changed(from, to)` y al arrancar (pantalla inicial): `clip` =
  `to == Playing ? nivel.musica : (to == Paused ? null : musica.Para(to))`. `Decidir(actualClip, clip)`:
  `Seguir` no hace nada; `Cruzar` arranca la fuente libre con el clip en volumen 0 y cruza en
  `musica.fundido` s.
- **Tensión:** al cruzar a un tema de nivel con `nivel.tension` asignada, la fuente de tensión
  arranca con `PlayScheduled` en el mismo `AudioSettings.dspTime + 0.1` que el tema, volumen 0.
  Si `tension.length` difiere de `musica.length` en más de 0,05 s, `Debug.LogWarning`. Durante
  `Playing`, `volumenPropio` sigue a `SetAmenaza` con `Mathf.SmoothDamp` (0,5 s). Al salir de
  `Playing`/`Paused` a otra pantalla, la tensión funde a 0 junto con el tema y se detiene.
  Volver de la pausa no reinicia nada.
- Reintentar (`Attempt` cambia, misma escena de nivel) no reinicia el tema (mismo clip → Seguir).
- [ ] **Steps:** implementar; en Play desde el menú: suena el tema del menú (`Actual.clip`),
  Personaje sigue con el mismo sin reiniciar (`time` sigue creciendo), la cinemática cruza, el
  nivel cruza a su tema y la tensión arranca sincronizada (`timeSamples` iguales ±1 % tras 1 s),
  `SetAmenaza(1)` la sube, al Game Over cruza y la tensión se apaga. (Con clips de prueba creados
  en memoria con `AudioClip.Create` si Task 7 aún no generó los placeholders.) Suite verde.
  **Commit** `Audio: musica por pantalla con fundido cruzado y capa de tension`.

---

### Task 6: Sonido del nivel

**Files:** Create `Scripts/Audio/SfxDirector.cs`, `Scripts/Audio/MotorSonido.cs`,
`Scripts/Audio/HordaSonido.cs`, `Scripts/Audio/AmbienteSonido.cs`, `Scripts/Audio/SonidoDeNivel.cs`
(prefab raíz); Create prefab `Art/Audio/SonidoDelNivel.prefab` (o `Audio/Prefabs/`); Modify
`Level_01.unity`, `Level_02.unity`, `Templates/NivelBase.unity` (una instancia del prefab, hija
de `Game` o suelta en la raíz — **nunca en `Nivel`**).

Comportamiento (spec §3–§5):
- `SonidoDeNivel` (raíz del prefab): en `OnEnable` pone `AudioDirector.Instance.BancoNivel` =
  `(GameRoot.Instance.CurrentLevel?.audio as AudioDeNivel)?.banco` y le pasa
  `worldUnitsPerMeter` (del `RunController`/config); en `OnDisable` lo limpia. Cada cuadro
  actualiza `AudioDirector.Instance.PlayerWorldX` con `run.ToWorldX(run.Sim.State.PlayerX)` y
  `MusicDirector.Instance?.SetAmenaza(1 - Clamp01(Gap / dangerGapMeters))` con
  `dangerGapMeters = 45`. Busca el `RunController` si no está cableado.
- `SfxDirector`: se suscribe a `run.Stepped` (como `GamepadRumble`) y por cada bandera del
  `RunEvent` pide su clave: `Shot→Disparo`, `ShotDenied→SinBalas`, `LaneChanged→CambioCarril`,
  `Crashed→Choque` (posición de la moto), `HeadlightOn→FaroOn`, `HeadlightOff→FaroOff`,
  `RanOutOfFuel→SinNafta`, `Launched→Salto`, `Landed→Aterrizaje`,
  `LandedPerfect→AterrizajePerfecto` (en vez de `Aterrizaje` si vienen juntos), `PickedUp→` la
  clave del recurso que subió respecto del paso anterior (guardar `Fuel`, `Battery`, `Ammo` del
  paso previo; si subieron varios, uno por cada uno). Para `RanOver` y `Explosion`, leer
  `run.Sim.Horde.Events` del mismo `Epoch` (como `FxManager`) y pedir `Atropello` /
  `ExplosionBarril` con `worldX = run.ToWorldX(e.X)` (posicionales). `Won`/`Lost` no: Victoria
  y Derrota suenan desde `SfxDirector` al ver el flujo pasar a `LevelComplete`/`GameOver`
  (`GameRoot.Flow.Changed`, desuscribir en `OnDisable`). Leer los nombres exactos de
  `HordeEventKind` en `HordeSimulation.cs`.
- `ZombieAdelante`: `SfxDirector` pide la clave, posicional, cuando un item `ZombieFront` del
  recorrido entra a `distanciaMaxima` por delante de la moto (una vez por item; los items y su
  distancia salen de `run.Level.Items` — ver cómo lo recorre `LevelScene.Bind`).
- `MotorSonido` (hijo del prefab): `AudioSource` loop + `FuenteConBus` (Efectos) con el `Sonido`
  `Motor` resuelto del banco (primer clip); `pitch` hacia `MotorTono.Objetivo(run.Sim.PlayerSpeed,
  config.normalSpeed, state.Mode == DriveMode.Turbo)` (leer el nombre real del modo turbo en
  `RunState.cs`) con `SmoothDamp` 0,15 s; `volumenPropio` a 0 en 1 s con `Fuel <= 0`; en `Lost`,
  a 0 en 0,3 s y `Stop`. Arranca al entrar a `Playing`.
- `HordaSonido` (hijo propio, sin hijos): loop `Horda`, posicional: `panStereo`/`espacial` con
  `dx = HordeX − PlayerX` (metros) cada cuadro. No mueve su transform (no hace falta).
- `AmbienteSonido`: loop del `AudioDeNivel.ambiente` por bus Ambiente, entra en 1 s.
- [ ] **Steps:** implementar, crear el prefab con los cuatro hijos, instanciarlo en las tres
  escenas (nunca bajo `Nivel`), extender `HordeGlowSceneTests` o un test nuevo
  `SonidoDeNivelSceneTests` que exija una instancia por escena de nivel y que no cuelgue de un
  objeto con `LevelScene`. Suite verde. Play desde el menú con un banco global de prueba (clips
  `AudioClip.Create`): disparo (`Shot`) reproduce `Disparo`, una explosión a la derecha da
  `panStereo > 0`, el motor sube el `pitch` con turbo, la horda panea a la izquierda, en la pausa
  `Buses.Gain(Efectos) == 0`. **Commit** `Audio: sonido del nivel (eventos, motor, horda, ambiente)`.

---

### Task 7: Placeholders, assets y cableado

**Files:** Create `Editor/Audio/PlaceholderAudioGenerator.cs` (menú `Zombineta > Audio > Generar
placeholders`), `Editor/Audio/AudioImportRules.cs` (`AssetPostprocessor`); crear los WAV en
`Audio/{Musica,Sfx,Ambiente}/`; crear `Settings/Audio/Musica.asset`, `SonidosGlobales.asset`,
`Espacial.asset`, un `Sonido` por clave en `Settings/Audio/Sonidos/`, y
`Settings/Audio/Nivel01.asset`, `Nivel02.asset` (`AudioDeNivel`); cablear `Boot` (director),
`Niveles.asset` (`audio` de cada nivel), `Musica.asset` en el `MusicDirector`.

- Generador: WAV 44,1 kHz 16 bit mono, escrito a mano (cabecera RIFF + PCM), sintetizado con
  `System.Math` (senoidales, cuadradas, ruido con envolvente, barridos). Uno distinto por archivo
  (cambiar frecuencia/timbre por clave y por variante). **No pisa archivos que existan.**
  Lista y duraciones:
  - Música (loop 16 s, fade-in/out de 5 ms en los bordes para que el loop no haga clic, y con la
    misma duración exacta tema y tensión): `mus_menu_loop`, `mus_character_loop`,
    `mus_cinematic_loop`, `mus_level1_loop`, `mus_level1_tension`, `mus_level2_loop`,
    `mus_level2_tension`, `mus_levelcomplete_loop`, `mus_gameover_loop`, `mus_ending_loop`,
    `mus_credits_loop`. (Opciones no tiene: sigue el tema del menú.)
  - Ambiente (loop 10 s): `amb_level1_city`, `amb_level2_bridge`.
  - Efectos (0,1–1,5 s; 2 variantes `_01`/`_02` salvo UI y loops): `sfx_player_shot`,
    `sfx_player_noammo`, `sfx_player_lane`, `sfx_player_crash`, `sfx_zombie_runover`,
    `sfx_barrel_explosion`, `sfx_pickup_fuel`, `sfx_pickup_battery`, `sfx_pickup_ammo`,
    `sfx_headlight_on`, `sfx_headlight_off`, `sfx_player_nofuel`, `sfx_player_jump`,
    `sfx_player_land`, `sfx_player_landperfect`, `sfx_stinger_win`, `sfx_stinger_lose`,
    `sfx_zombie_groan`, `sfx_ui_move_01`, `sfx_ui_confirm_01`, `sfx_ui_back_01`; loops de 4 s:
    `sfx_engine_loop_01`, `sfx_horde_loop_01`.
  - Aleatorios de pantalla: `sfx_screen_groan_01`, `_02` (Game Over), `sfx_screen_wind_01`
    (Ending, Créditos).
- `AudioImportRules`: bajo `Audio/Musica` y `Audio/Ambiente` → `Streaming`, `Vorbis`, calidad
  0,7; bajo `Audio/Sfx` → `DecompressOnLoad`, `ADPCM`, `loadInBackground = false`. Solo en la
  primera importación (`assetImporter.importSettingsMissing`), para no pisar un ajuste manual.
- Assets: un `Sonido` por clave con sus clips; `Motor`/`Horda` con `loop = true`;
  `ExplosionBarril`, `Choque`, `Atropello`, `ZombieAdelante`, `Horda` con `posicional = true`;
  UI con `bus = UI`, volumen 0,6. Volúmenes por defecto moderados (0,6–0,8) para que los
  placeholders no cansen. `Nivel02.asset` pisa `ExplosionBarril` con otra variante para que se
  note que el banco del nivel manda.
- `SonidosAleatorios` de GameOver/Ending/Credits con sus `Sonido`.
- [ ] **Steps:** implementar, correr el menú, verificar que se crearon todos los archivos con las
  duraciones pedidas (leer `AudioClip.length` de cada uno) y los ajustes de importación, correr
  el menú una segunda vez y verificar que no pisó nada (fecha de modificación igual), cablear
  todo. Suite verde. **Commit** `Audio: placeholders generados, sonidos y bancos cableados`.

---

### Task 8: Verificación, guía y HANDOFF

- [ ] **Step 1: Play desde el menú, todo el recorrido del spec §8**, leyendo el estado de las
  fuentes en cada paso y anotándolo: tema del menú; Personaje sigue sin reinicio; cinemática
  cruza; nivel con tema + tensión sincronizados, tensión sube al acercar la horda (forzar
  `HordeX` cerca); un disparo y una explosión (forzar con `F2`/eventos como hizo la verificación
  de iluminación) suenan con el paneo del lado correcto; en la pausa los efectos callan y la
  música queda a 0,4; los tres sliders cambian `Buses`; Game Over, Ending y Créditos con su tema;
  Créditos desde el menú y desde el Ending; UI suena al mover y confirmar. Consola sin errores.
- [ ] **Step 2: `docs/Guia-de-sonido.md`** (castellano, para quien hace el sonido, sin código):
  qué archivos hay y dónde, la tabla completa de archivos con duración/loop/variantes, formatos
  (efectos WAV 44,1 kHz 16 bit; música y ambiente OGG o WAV), cómo reemplazar (pisar con el mismo
  nombre; variantes nuevas `_03` + sumarlas a `clips`), cómo hacer un loop sin clic y por qué
  tema y tensión deben medir exactamente lo mismo, cómo ajustar volumen/tono/cooldown en cada
  `Sonido`, cómo probar cada cosa (escena y acción), qué no tocar.
- [ ] **Step 3: HANDOFF:** sección "Sonido (`Juego/`)" en la sección 0 (arquitectura en 10
  líneas, dónde está cada cosa, buses sin mixer y por qué, trampa: nunca asignar
  `AudioSource.volume` directo); Créditos en la tabla de escenas y en el flujo; verificado en
  sección 6; pendiente en sección 7 (sonido final, que José lo juegue con auriculares).
- [ ] **Step 4: Commit** `Audio: verificacion, guia de sonido y HANDOFF`.
