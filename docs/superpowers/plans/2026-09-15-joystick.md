# Jugar con joystick — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que un nivel de `Juego/` se juegue entero con joystick (gatillos para la velocidad), con
vibración opcional, atajos de depuración y ayuda de controles en la pausa, sin romper teclado y
mouse.

**Architecture:** Los controles pasan a un mapa de acciones `Moto` del Input System;
`PlayerInputReader` arma el mismo `PlayerIntent` leyendo acciones. La vibración es un
`RumbleDirector` en C# plano (testeado) que una vista aplica al joystick. El último dispositivo
usado lo detecta un `InputDeviceTracker` y lo muestra `ControlHints` en la pausa. Tests EditMode
con dispositivos virtuales (`InputTestFixture`).

**Tech Stack:** Unity 6000.6.0f1, Input System 1.20.0 (+ TestFramework), uGUI, NUnit, MCP de Unity.

Spec: `docs/superpowers/specs/2026-09-15-joystick-design.md`.

**Nota:** como en los planes anteriores, el código de vistas y el cableado de escenas se hace
directo (por MCP para escenas); el plan fija archivos, interfaces, el código de la lógica
testeable y los tests.

## Global Constraints

- Solo `Juego/`. No tocar `Prototipo/` ni la lógica de reglas de
  `Juego/Assets/_Zombineta/Simulacion/Runtime` (sí se agrega `GameSettings.Vibration`, que es
  preferencia y no regla).
- Esquema joystick: carril = stick izq. arriba/abajo o D-pad; Turbo = gatillo derecho; Retroceso =
  gatillo izquierdo; Disparar = botón oeste (X/□) o hombro derecho (RB/R1); Faro = botón norte
  (Y/△); Pausa = Start (ya existe).
- Umbrales: stick de carril con punto de presión 0,5 (liberación ≈ 0,375, default del Input
  System); gatillos apretados desde 0,3.
- Teclado/mouse conservan: W/↑ S/↓ carril, D/→ turbo, A/← retroceso, Espacio faro, X o click
  izquierdo disparar, Esc pausa, R reinicio rápido, F2 ganar, F3 perder.
- F2/F3 joystick: View/Select mantenido + RB gana, + LB pierde; solo `UNITY_EDITOR || DEVELOPMENT_BUILD`.
- `GameSettings.Vibration`: bool, default true, clave `zombineta.vibration`.
- Textos de ayuda (verbatim):
  - Teclado: `W/S o ↑/↓ carril · D turbo · A retroceso · X o click disparar · Espacio faro · Esc pausa`
  - Joystick: `Stick o cruceta carril · RT turbo · LT retroceso · X o RB disparar · Y faro · Start pausa`
  - Segunda línea (ambos): `En el aire, turbo y retroceso inclinan la moto.`
- Estáticos reiniciados en `SubsystemRegistration` (recarga de dominio desactivada).
- Commits en español con `Co-Authored-By` del modelo que los escribe. Push/merge solo si el
  usuario lo pide.

Rutas: `Z/` = `Juego/Assets/_Zombineta/`; `A` = `Juego/Assets/Settings/InputSystem_Actions.inputactions`.

---

### Task 1: Mapa de acciones `Moto` y `PlayerInputReader` por acciones

**Files:**
- Modify: `Juego/Packages/manifest.json` (agregar `"testables": ["com.unity.inputsystem"]`)
- Modify: `Z/Tests/Editor/Zombineta.Juego.Tests.asmdef` (referencias `Unity.InputSystem`,
  `Unity.InputSystem.TestFramework`)
- Modify: `A` (mapa `Moto` nuevo; los mapas `Player` y `UI` no cambian)
- Modify: `Z/Scripts/Gameplay/Player/PlayerInputReader.cs`
- Modify: `Z/Scripts/Levels/LevelFlowBridge.cs` (F2/F3 por el reader)
- Modify (MCP): `Z/Scenes/Templates/NivelBase.unity`, `Z/Scenes/Level_01.unity`,
  `Z/Scenes/Level_02.unity` (asignar el asset de acciones al reader y el reader al bridge)
- Test: `Z/Tests/Editor/PlayerInputReaderTests.cs`

**Interfaces:**
- Produces:
  - Mapa `Moto` con acciones: `LaneUp` (Button), `LaneDown` (Button), `Turbo` (Value, Axis),
    `Reverse` (Value, Axis), `Fire` (Button), `Headlight` (Button), `QuickRestart` (Button),
    `DebugWin` (Button), `DebugLose` (Button).
  - `PlayerInputReader`: `[SerializeField] InputActionAsset actions`;
    `void Bind(InputActionMap moto)` (habilita el mapa; lo usa `OnEnable` con
    `actions.FindActionMap("Moto", true)` y los tests);
    `PlayerIntent Read()`; `bool RestartPressed`; `bool DebugWinPressed`; `bool DebugLosePressed`;
    `const float TriggerThreshold = 0.3f`.

**Bindings exactos del mapa `Moto`:**

| Acción | Bindings |
|---|---|
| `LaneUp` | `<Keyboard>/w`, `<Keyboard>/upArrow`, `<Gamepad>/leftStick/up`, `<Gamepad>/dpad/up` |
| `LaneDown` | `<Keyboard>/s`, `<Keyboard>/downArrow`, `<Gamepad>/leftStick/down`, `<Gamepad>/dpad/down` |
| `Turbo` | `<Keyboard>/d`, `<Keyboard>/rightArrow`, `<Gamepad>/rightTrigger` |
| `Reverse` | `<Keyboard>/a`, `<Keyboard>/leftArrow`, `<Gamepad>/leftTrigger` |
| `Fire` | `<Keyboard>/x`, `<Mouse>/leftButton`, `<Gamepad>/buttonWest`, `<Gamepad>/rightShoulder` |
| `Headlight` | `<Keyboard>/space`, `<Gamepad>/buttonNorth` |
| `QuickRestart` | `<Keyboard>/r` |
| `DebugWin` | `<Keyboard>/f2`; composite `OneModifier` (modifier `<Gamepad>/select`, binding `<Gamepad>/rightShoulder`) |
| `DebugLose` | `<Keyboard>/f3`; composite `OneModifier` (modifier `<Gamepad>/select`, binding `<Gamepad>/leftShoulder`) |

Editar `A` por código en el editor (no a mano): cargar el JSON con
`InputActionAsset.FromJson(File.ReadAllText(path))`, agregar el mapa con
`AddActionMap`/`AddAction`/`AddBinding`/`AddCompositeBinding("OneModifier").With("Modifier", ...).With("Binding", ...)`,
escribir `asset.ToJson()` al mismo archivo y `AssetDatabase.ImportAsset`. Verificar que el GUID del
`.meta` no cambió y que `Player`/`UI` siguen iguales (diff del JSON).

- [ ] **Step 1: Infra de tests** — `testables` en el manifest y referencias en el asmdef de tests;
  Refresh; consola sin errores; 186 tests siguen en verde.

- [ ] **Step 2: Tests que fallan**

```csharp
using NUnit.Framework;
using UnityEditor;
using UnityEngine;
using UnityEngine.InputSystem;
using Zombineta.Core;
using Zombineta.Player;

namespace Zombineta.Juego.Tests
{
    public class PlayerInputReaderTests : InputTestFixture
    {
        const string ActionsPath = "Assets/Settings/InputSystem_Actions.inputactions";

        Gamepad pad;
        Keyboard kb;
        Mouse mouse;
        InputActionAsset actions;
        GameObject go;
        PlayerInputReader reader;

        public override void Setup()
        {
            base.Setup();
            pad = InputSystem.AddDevice<Gamepad>();
            kb = InputSystem.AddDevice<Keyboard>();
            mouse = InputSystem.AddDevice<Mouse>();

            // Copia del asset: los tests no habilitan el asset compartido del proyecto.
            var source = AssetDatabase.LoadAssetAtPath<InputActionAsset>(ActionsPath);
            actions = InputActionAsset.FromJson(source.ToJson());

            go = new GameObject("reader");
            reader = go.AddComponent<PlayerInputReader>();
            reader.Bind(actions.FindActionMap("Moto", true));
        }

        public override void TearDown()
        {
            Object.DestroyImmediate(go);
            Object.DestroyImmediate(actions);
            base.TearDown();
        }

        // --- Joystick ---------------------------------------------------------------

        [Test]
        public void StickUp_ChangesOneLane_EvenIfHeld()
        {
            Set(pad.leftStick, new Vector2(0f, 0.9f));
            Assert.AreEqual(1, reader.Read().LaneDelta);
            InputSystem.Update();
            Assert.AreEqual(0, reader.Read().LaneDelta);
        }

        [Test]
        public void StickMustComeBackBeforeTheNextLane()
        {
            Set(pad.leftStick, new Vector2(0f, 0.9f));
            reader.Read();
            Set(pad.leftStick, new Vector2(0f, 0.45f));   // todavia por encima de la liberacion
            Set(pad.leftStick, new Vector2(0f, 0.9f));
            Assert.AreEqual(0, reader.Read().LaneDelta);
            Set(pad.leftStick, new Vector2(0f, 0.1f));
            Set(pad.leftStick, new Vector2(0f, 0.9f));
            Assert.AreEqual(1, reader.Read().LaneDelta);
        }

        [Test]
        public void ASoftStickPush_DoesNotChangeLane()
        {
            Set(pad.leftStick, new Vector2(0f, 0.4f));
            Assert.AreEqual(0, reader.Read().LaneDelta);
        }

        [Test]
        public void DpadDown_GoesDownOneLane()
        {
            Press(pad.dpad.down);
            Assert.AreEqual(-1, reader.Read().LaneDelta);
        }

        [Test]
        public void RightTrigger_IsTurbo_FromThreshold()
        {
            Set(pad.rightTrigger, 0.2f);
            Assert.AreEqual(DriveMode.Normal, reader.Read().Mode);
            Set(pad.rightTrigger, 0.35f);
            Assert.AreEqual(DriveMode.Turbo, reader.Read().Mode);
        }

        [Test]
        public void LeftTrigger_IsReverse()
        {
            Set(pad.leftTrigger, 1f);
            Assert.AreEqual(DriveMode.Reverse, reader.Read().Mode);
        }

        [Test]
        public void BothTriggers_AreNormal()
        {
            Set(pad.leftTrigger, 1f);
            Set(pad.rightTrigger, 1f);
            Assert.AreEqual(DriveMode.Normal, reader.Read().Mode);
        }

        [Test]
        public void WestButtonAndRightShoulder_Fire()
        {
            Press(pad.buttonWest);
            Assert.IsTrue(reader.Read().Fire);
            Release(pad.buttonWest);
            Press(pad.rightShoulder);
            Assert.IsTrue(reader.Read().Fire);
        }

        [Test]
        public void NorthButton_TogglesTheHeadlight()
        {
            Press(pad.buttonNorth);
            Assert.IsTrue(reader.Read().ToggleHeadlight);
        }

        [Test]
        public void DebugWin_NeedsSelectHeld()
        {
            Press(pad.rightShoulder);
            Assert.IsFalse(reader.DebugWinPressed);
            Release(pad.rightShoulder);
            Press(pad.selectButton);
            Press(pad.rightShoulder);
            Assert.IsTrue(reader.DebugWinPressed);
        }

        [Test]
        public void DebugLose_IsSelectPlusLeftShoulder()
        {
            Press(pad.selectButton);
            Press(pad.leftShoulder);
            Assert.IsTrue(reader.DebugLosePressed);
        }

        // --- Teclado y mouse (lo de siempre sigue andando) --------------------------

        [Test]
        public void Keyboard_LanesModesHeadlightAndFire()
        {
            Press(kb.wKey);
            Assert.AreEqual(1, reader.Read().LaneDelta);
            Release(kb.wKey);

            Press(kb.dKey);
            Assert.AreEqual(DriveMode.Turbo, reader.Read().Mode);
            Press(kb.aKey);
            Assert.AreEqual(DriveMode.Normal, reader.Read().Mode);
            Release(kb.dKey);
            Assert.AreEqual(DriveMode.Reverse, reader.Read().Mode);
            Release(kb.aKey);

            Press(kb.spaceKey);
            Assert.IsTrue(reader.Read().ToggleHeadlight);
            Press(kb.xKey);
            Assert.IsTrue(reader.Read().Fire);
        }

        [Test]
        public void MouseClick_Fires()
        {
            Press(mouse.leftButton);
            Assert.IsTrue(reader.Read().Fire);
        }

        [Test]
        public void F2AndR_StillWork()
        {
            Press(kb.f2Key);
            Assert.IsTrue(reader.DebugWinPressed);
            Press(kb.rKey);
            Assert.IsTrue(reader.RestartPressed);
        }
    }
}
```

- [ ] **Step 3: Correr y ver que falla** (compilación: `Bind`, `DebugWinPressed`, etc. no existen;
  mapa `Moto` inexistente).

- [ ] **Step 4: Agregar el mapa `Moto` a `A`** (código de editor, tabla de bindings).

- [ ] **Step 5: Implementar `PlayerInputReader`**

```csharp
using UnityEngine;
using UnityEngine.InputSystem;
using Zombineta.Core;

namespace Zombineta.Player
{
    /// <summary>
    /// Traduce los controles (teclado, mouse o joystick) a un PlayerIntent. Lee el mapa de
    /// acciones "Moto": que tecla o boton hace que vive en el asset de acciones, no aca.
    /// </summary>
    public sealed class PlayerInputReader : MonoBehaviour
    {
        /// <summary>Desde cuanto cuenta un gatillo como apretado.</summary>
        public const float TriggerThreshold = 0.3f;

        [SerializeField] InputActionAsset actions;

        InputActionMap map;
        InputAction laneUp, laneDown, turbo, reverse, fire, headlight, quickRestart, debugWin, debugLose;

        void OnEnable()
        {
            if (map == null && actions != null)
                Bind(actions.FindActionMap("Moto", true));
            map?.Enable();
        }

        void OnDisable() => map?.Disable();

        public void Bind(InputActionMap moto)
        {
            map = moto;
            laneUp = moto.FindAction("LaneUp", true);
            laneDown = moto.FindAction("LaneDown", true);
            turbo = moto.FindAction("Turbo", true);
            reverse = moto.FindAction("Reverse", true);
            fire = moto.FindAction("Fire", true);
            headlight = moto.FindAction("Headlight", true);
            quickRestart = moto.FindAction("QuickRestart", true);
            debugWin = moto.FindAction("DebugWin", true);
            debugLose = moto.FindAction("DebugLose", true);
            moto.Enable();
        }

        public PlayerIntent Read()
        {
            var intent = PlayerIntent.Idle;
            if (map == null)
                return intent;

            if (laneUp.WasPressedThisFrame())
                intent.LaneDelta += 1;
            if (laneDown.WasPressedThisFrame())
                intent.LaneDelta -= 1;

            bool isTurbo = turbo.ReadValue<float>() >= TriggerThreshold;
            bool isReverse = reverse.ReadValue<float>() >= TriggerThreshold;
            if (isTurbo && !isReverse)
                intent.Mode = DriveMode.Turbo;
            else if (isReverse && !isTurbo)
                intent.Mode = DriveMode.Reverse;

            intent.ToggleHeadlight = headlight.WasPressedThisFrame();
            intent.Fire = fire.WasPressedThisFrame();
            return intent;
        }

        public bool RestartPressed => quickRestart != null && quickRestart.WasPressedThisFrame();
        public bool DebugWinPressed => debugWin != null && debugWin.WasPressedThisFrame();
        public bool DebugLosePressed => debugLose != null && debugLose.WasPressedThisFrame();
    }
}
```

- [ ] **Step 6: Correr: PASS** (14 tests nuevos) y total 200 en verde. Si un caso del stick falla
  por cómo el Input System trata `WasPressedThisFrame` en controles analógicos, ajustar el binding
  (p. ej. interacción `Press(pressPoint=0.5)`) — no el test — y anotarlo en el reporte.

- [ ] **Step 7: `LevelFlowBridge`** — campo `[SerializeField] PlayerInputReader input`; dentro del
  bloque `#if UNITY_EDITOR || DEVELOPMENT_BUILD` reemplazar `kb.f2Key.wasPressedThisFrame` por
  `input.DebugWinPressed` y `kb.f3Key` por `input.DebugLosePressed` (con `input != null`). Quitar
  `using UnityEngine.InputSystem` si queda sin uso (la acción Pause sigue usándolo).

- [ ] **Step 8: Escenas por MCP** — en `NivelBase`, `Level_01`, `Level_02`: asignar `A` a
  `PlayerInputReader.actions` y el reader a `LevelFlowBridge.input`; guardar. Verificar conteo de
  `LevelItem` (191 / 206) sin cambios.

- [ ] **Step 9: Play por MCP** (callback antes de Play) en `Level_01` con
  `InputSystem.AddDevice<Gamepad>("PadVirtual")` y estados encolados
  (`InputSystem.QueueStateEvent(pad, new GamepadState { ... })` + esperar frames): stick arriba →
  `State.Lane` sube 1; RT → `State.Mode == Turbo`; X → munición baja o evento Shot; Y → faro
  cambia; teclado W sigue cambiando carril. Quitar el dispositivo al final
  (`InputSystem.RemoveDevice`). Consola sin errores.

- [ ] **Step 10: Commit**

```bash
git add Juego/Packages/manifest.json Juego/Assets/Settings/InputSystem_Actions.inputactions Juego/Assets/_Zombineta/Scripts/Gameplay/Player/PlayerInputReader.cs Juego/Assets/_Zombineta/Scripts/Levels/LevelFlowBridge.cs Juego/Assets/_Zombineta/Tests/Editor Juego/Assets/_Zombineta/Scenes
git commit -m "Controles por acciones: jugar con joystick"
```

---

### Task 2: Vibración

**Files:**
- Create: `Z/Scripts/Gameplay/Fx/RumbleKind.cs` (enum), `Z/Scripts/Gameplay/Fx/RumbleConfig.cs`,
  `Z/Scripts/Gameplay/Fx/RumbleDirector.cs`, `Z/Scripts/Gameplay/Fx/GamepadRumble.cs`
- Create: `Z/Settings/Rumble.asset`
- Modify: `Z/Simulacion/Runtime/Core/GameSettings.cs` (`Vibration`)
- Modify: `Z/Scripts/Screens/OptionsScreen.cs`; (MCP) `Z/Scenes/Options.unity` (interruptor)
- Modify (MCP): `NivelBase`, `Level_01`, `Level_02` (componente `GamepadRumble` en el objeto
  `Nivel`, cableado)
- Test: `Z/Tests/Editor/RumbleDirectorTests.cs`

**Interfaces:**
- Produces:
  - `enum RumbleKind { Shot, Crash, Fall, RanOver, Explosion, Caught }` (namespace `Zombineta.Fx`)
  - `RumbleConfig : ScriptableObject` con `[Serializable] class Entry { RumbleKind kind; float low; float high; float seconds; }`,
    `List<Entry> entries`, `Entry Get(RumbleKind)` (null si no está), `void Set(RumbleKind, float low, float high, float seconds)`.
  - `RumbleDirector(RumbleConfig config)`: `void Trigger(RumbleKind)`, `void Trigger(RunEvent events)`,
    `void Tick(float dt)`, `float Low`, `float High`, `void Stop()`.
  - `GameSettings.Vibration` (bool).

- [ ] **Step 1: Test que falla**

```csharp
using NUnit.Framework;
using UnityEngine;
using Zombineta.Core;
using Zombineta.Fx;

namespace Zombineta.Juego.Tests
{
    public class RumbleDirectorTests
    {
        RumbleConfig config;

        [SetUp]
        public void SetUp()
        {
            config = ScriptableObject.CreateInstance<RumbleConfig>();
            config.Set(RumbleKind.Crash, 0.6f, 0.4f, 0.25f);
            config.Set(RumbleKind.Shot, 0.05f, 0.5f, 0.1f);
            config.Set(RumbleKind.Caught, 1f, 0.8f, 0.9f);
        }

        [TearDown]
        public void TearDown() => Object.DestroyImmediate(config);

        [Test]
        public void AnEvent_StartsAtItsStrength_AndFadesToZero()
        {
            var d = new RumbleDirector(config);
            d.Trigger(RumbleKind.Crash);
            d.Tick(0f);
            Assert.AreEqual(0.6f, d.Low, 1e-4f);
            Assert.AreEqual(0.4f, d.High, 1e-4f);

            d.Tick(0.125f);
            Assert.AreEqual(0.3f, d.Low, 1e-4f);

            d.Tick(0.2f);
            Assert.AreEqual(0f, d.Low);
            Assert.AreEqual(0f, d.High);
        }

        [Test]
        public void TwoEvents_EachMotorTakesTheStrongest()
        {
            var d = new RumbleDirector(config);
            d.Trigger(RumbleKind.Crash);
            d.Trigger(RumbleKind.Shot);
            d.Tick(0f);
            Assert.AreEqual(0.6f, d.Low, 1e-4f);
            Assert.AreEqual(0.5f, d.High, 1e-4f);
        }

        [Test]
        public void RetriggeringTheSameKind_RestartsItsFade()
        {
            var d = new RumbleDirector(config);
            d.Trigger(RumbleKind.Crash);
            d.Tick(0.2f);
            d.Trigger(RumbleKind.Crash);
            d.Tick(0f);
            Assert.AreEqual(0.6f, d.Low, 1e-4f);
        }

        [Test]
        public void Stop_SilencesEverything()
        {
            var d = new RumbleDirector(config);
            d.Trigger(RumbleKind.Caught);
            d.Stop();
            d.Tick(0f);
            Assert.AreEqual(0f, d.Low);
            Assert.AreEqual(0f, d.High);
        }

        [Test]
        public void AKindWithoutConfig_DoesNothing()
        {
            var d = new RumbleDirector(config);
            d.Trigger(RumbleKind.Explosion);
            d.Tick(0f);
            Assert.AreEqual(0f, d.Low);
        }

        [Test]
        public void RunEvents_MapToTheirRumble()
        {
            var d = new RumbleDirector(config);
            d.Trigger(RunEvent.Crashed | RunEvent.Shot | RunEvent.PickedUp);
            d.Tick(0f);
            Assert.AreEqual(0.6f, d.Low, 1e-4f);
            Assert.AreEqual(0.5f, d.High, 1e-4f);
        }
    }
}
```

- [ ] **Step 2: Correr y ver que falla** (compilación).

- [ ] **Step 3: Implementar**

```csharp
namespace Zombineta.Fx
{
    public enum RumbleKind { Shot, Crash, Fall, RanOver, Explosion, Caught }
}
```

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;

namespace Zombineta.Fx
{
    /// <summary>Cuanto vibra cada cosa: motor grave (low), agudo (high) y duracion.</summary>
    [CreateAssetMenu(menuName = "Zombineta/Vibracion", fileName = "Rumble")]
    public sealed class RumbleConfig : ScriptableObject
    {
        [Serializable]
        public sealed class Entry
        {
            public RumbleKind kind;
            [Range(0f, 1f)] public float low;
            [Range(0f, 1f)] public float high;
            [Min(0.01f)] public float seconds = 0.2f;
        }

        public List<Entry> entries = new List<Entry>();

        public Entry Get(RumbleKind kind)
        {
            foreach (var e in entries)
                if (e.kind == kind)
                    return e;
            return null;
        }

        public void Set(RumbleKind kind, float low, float high, float seconds)
        {
            var e = Get(kind);
            if (e == null)
                entries.Add(e = new Entry { kind = kind });
            e.low = low;
            e.high = high;
            e.seconds = seconds;
        }
    }
}
```

```csharp
using System;
using UnityEngine;
using Zombineta.Core;

namespace Zombineta.Fx
{
    /// <summary>
    /// La vibracion del joystick sin tocar el joystick: cada evento arranca a su fuerza y se apaga
    /// solo; si hay varios, cada motor toma el mas fuerte. GamepadRumble aplica Low/High.
    /// </summary>
    public sealed class RumbleDirector
    {
        static readonly int KindCount = Enum.GetValues(typeof(RumbleKind)).Length;

        readonly RumbleConfig config;
        readonly float[] left = new float[KindCount];

        public float Low { get; private set; }
        public float High { get; private set; }

        public RumbleDirector(RumbleConfig config) => this.config = config;

        public void Trigger(RumbleKind kind)
        {
            var e = config != null ? config.Get(kind) : null;
            if (e != null)
                left[(int)kind] = e.seconds;
        }

        public void Trigger(RunEvent events)
        {
            if ((events & RunEvent.Shot) != 0) Trigger(RumbleKind.Shot);
            if ((events & RunEvent.Crashed) != 0) Trigger(RumbleKind.Crash);
            if ((events & RunEvent.Fell) != 0) Trigger(RumbleKind.Fall);
            if ((events & RunEvent.RanOver) != 0) Trigger(RumbleKind.RanOver);
            if ((events & RunEvent.Explosion) != 0) Trigger(RumbleKind.Explosion);
        }

        public void Tick(float dt)
        {
            float low = 0f, high = 0f;
            for (int i = 0; i < KindCount; i++)
            {
                if (left[i] <= 0f)
                    continue;
                // Primero se descuenta el tiempo y despues se mide: Tick(0) da la fuerza entera.
                left[i] = Mathf.Max(0f, left[i] - dt);
                if (left[i] <= 0f)
                    continue;
                var e = config.Get((RumbleKind)i);
                float k = Mathf.Clamp01(left[i] / e.seconds);
                low = Mathf.Max(low, e.low * k);
                high = Mathf.Max(high, e.high * k);
            }
            Low = low;
            High = high;
        }

        public void Stop()
        {
            Array.Clear(left, 0, left.Length);
            Low = 0f;
            High = 0f;
        }
    }
}
```

  Con el test `AnEvent_StartsAtItsStrength_AndFadesToZero`: `Tick(0)` → 0,6; `Tick(0,125)` → 0,3;
  `Tick(0,2)` → 0.

- [ ] **Step 4: Correr: PASS** (6 tests).

- [ ] **Step 5: `GameSettings.Vibration`** — mismo patrón que `Gore` (con `PlayerPrefs.Save()`),
  clave `zombineta.vibration`, default true.

- [ ] **Step 6: `GamepadRumble`**
  - Campos: `RunController run`, `RumbleConfig config`.
  - `Awake`: `director = new RumbleDirector(config)`. `OnEnable`: suscribir `run.Stepped +=
    director.Trigger` (vía lambda o método). `OnDisable`/`OnDestroy`: desuscribir, `Silence()`.
  - `Update`: si `!GameSettings.Vibration` o `GameRoot.Flow?.Current != GameScreen.Playing` y la
    partida no terminó en atrapada → `Silence()` y return. Si `Sim.State.Phase == Lost` y
    `Loss == CaughtByHorde` la primera vez → `director.Trigger(RumbleKind.Caught)` (la vibración de
    atrapada sigue durante la cámara lenta, antes de que el flujo cambie de pantalla).
    `director.Tick(Time.unscaledDeltaTime)`; si `Low`/`High` cambiaron respecto de lo último
    aplicado, `Gamepad.current?.SetMotorSpeeds(Low, High)`.
  - `OnApplicationFocus(false)` → `Silence()`. `Silence()` = `director.Stop()` +
    `Gamepad.current?.SetMotorSpeeds(0,0)` (y `ResetHaptics()` si hay joystick).
  - Valores de `Rumble.asset` (low / high / seconds): Shot 0,05/0,15/0,08; Crash 0,6/0,4/0,25;
    Fall 0,7/0,5/0,35; RanOver 0,4/0,6/0,2; Explosion 0,9/0,6/0,45; Caught 1/0,8/0,9.

- [ ] **Step 7: Opciones** — `OptionsScreen`: `[SerializeField] Toggle vibration;` con listener
  `GameSettings.Vibration = v` y `SetIsOnWithoutNotify(GameSettings.Vibration)`. Por MCP en
  `Options.unity`: duplicar el interruptor de `Gore` como "Vibración" debajo, cablear, y encadenar la
  navegación explícita (arriba/abajo) si los otros la usan. Guardar.

- [ ] **Step 8: Escenas de nivel por MCP**: `GamepadRumble` en `Nivel` con `run` y `Rumble.asset`.

- [ ] **Step 9: Play por MCP** con joystick virtual: forzar un choque (poner la moto a 1,5 m de un
  obstáculo de su carril, como en pruebas anteriores) y muestrear `director` vía un accesor
  `#if UNITY_EDITOR` (`float DebugLow/DebugHigh`): > 0 y luego 0; pausar → 0; con
  `GameSettings.Vibration = false` no sube. Consola sin errores. Restaurar `Vibration = true`.

- [ ] **Step 10: Commit**

```bash
git add Juego/Assets/_Zombineta/Scripts/Gameplay/Fx Juego/Assets/_Zombineta/Settings/Rumble.asset* Juego/Assets/_Zombineta/Simulacion/Runtime/Core/GameSettings.cs Juego/Assets/_Zombineta/Scripts/Screens/OptionsScreen.cs Juego/Assets/_Zombineta/Scenes Juego/Assets/_Zombineta/Tests/Editor/RumbleDirectorTests.cs*
git commit -m "Vibracion del joystick con opcion"
```

---

### Task 3: Ayuda de controles según el último dispositivo

**Files:**
- Create: `Z/Scripts/Gameplay/Player/InputDeviceTracker.cs`
- Create: `Z/Scripts/Screens/ControlHints.cs`
- Modify (MCP): `Z/Scenes/Pause.unity` (texto de ayuda con `ControlHints`)
- Test: `Z/Tests/Editor/InputDeviceTrackerTests.cs`

**Interfaces:**
- Produces:
  - `enum ControlScheme { KeyboardMouse, Gamepad }` (namespace `Zombineta.Player`)
  - `sealed class InputDeviceTracker`: `ControlScheme Current`, `event Action<ControlScheme> Changed`,
    `void Attach()`, `void Detach()`, `static ControlScheme SchemeOf(InputDevice device)`,
    `static InputDeviceTracker Shared` (creado y adjuntado en `SubsystemRegistration` +
    `AfterSceneLoad`; en editor se desadjunta al salir de Play).
  - `ControlHints : MonoBehaviour`: `[SerializeField] Text text`; `static string TextFor(ControlScheme)`.

- [ ] **Step 1: Tests que fallan**

```csharp
using NUnit.Framework;
using UnityEngine.InputSystem;
using Zombineta.Juego.Screens;
using Zombineta.Player;

namespace Zombineta.Juego.Tests
{
    public class InputDeviceTrackerTests : InputTestFixture
    {
        [Test]
        public void DevicesMapToTheirScheme()
        {
            Assert.AreEqual(ControlScheme.Gamepad, InputDeviceTracker.SchemeOf(InputSystem.AddDevice<Gamepad>()));
            Assert.AreEqual(ControlScheme.KeyboardMouse, InputDeviceTracker.SchemeOf(InputSystem.AddDevice<Keyboard>()));
            Assert.AreEqual(ControlScheme.KeyboardMouse, InputDeviceTracker.SchemeOf(InputSystem.AddDevice<Mouse>()));
        }

        [Test]
        public void UsingAnAction_SwitchesTheCurrentScheme()
        {
            var pad = InputSystem.AddDevice<Gamepad>();
            var kb = InputSystem.AddDevice<Keyboard>();
            var action = new InputAction("probe", InputActionType.Button);
            action.AddBinding("<Gamepad>/buttonSouth");
            action.AddBinding("<Keyboard>/space");
            action.Enable();

            var tracker = new InputDeviceTracker();
            tracker.Attach();
            ControlScheme? changedTo = null;
            tracker.Changed += s => changedTo = s;

            Press(pad.buttonSouth);
            Assert.AreEqual(ControlScheme.Gamepad, tracker.Current);
            Assert.AreEqual(ControlScheme.Gamepad, changedTo);

            Release(pad.buttonSouth);
            Press(kb.spaceKey);
            Assert.AreEqual(ControlScheme.KeyboardMouse, tracker.Current);

            tracker.Detach();
            action.Dispose();
        }

        [Test]
        public void HintsText_MatchesTheScheme()
        {
            StringAssert.StartsWith("W/S o ↑/↓ carril", ControlHints.TextFor(ControlScheme.KeyboardMouse));
            StringAssert.StartsWith("Stick o cruceta carril", ControlHints.TextFor(ControlScheme.Gamepad));
            StringAssert.Contains("En el aire, turbo y retroceso inclinan la moto.", ControlHints.TextFor(ControlScheme.Gamepad));
        }
    }
}
```

  (Si `ControlHints` vive en otro namespace, ajustar el `using`; revisar el namespace de
  `PauseScreen`.)

- [ ] **Step 2: Correr y ver que falla** (compilación).

- [ ] **Step 3: Implementar `InputDeviceTracker`**

```csharp
using System;
using UnityEngine;
using UnityEngine.InputSystem;

namespace Zombineta.Player
{
    public enum ControlScheme { KeyboardMouse, Gamepad }

    /// <summary>
    /// Recuerda con que se jugo por ultima vez (teclado/mouse o joystick) mirando las acciones que
    /// se ejecutan. La ayuda de controles lo usa para mostrar los botones correctos.
    /// </summary>
    public sealed class InputDeviceTracker
    {
        public static InputDeviceTracker Shared { get; private set; }

        public ControlScheme Current { get; private set; } = ControlScheme.KeyboardMouse;
        public event Action<ControlScheme> Changed;

        bool attached;

        [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]
        static void ResetShared()
        {
            Shared?.Detach();
            Shared = new InputDeviceTracker();
            Shared.Attach();
        }

        public static ControlScheme SchemeOf(InputDevice device) =>
            device is Gamepad || device is Joystick ? ControlScheme.Gamepad : ControlScheme.KeyboardMouse;

        public void Attach()
        {
            if (attached) return;
            InputSystem.onActionChange += OnActionChange;
            attached = true;
        }

        public void Detach()
        {
            if (!attached) return;
            InputSystem.onActionChange -= OnActionChange;
            attached = false;
        }

        void OnActionChange(object obj, InputActionChange change)
        {
            if (change != InputActionChange.ActionPerformed || !(obj is InputAction action))
                return;
            var device = action.activeControl?.device;
            if (device == null)
                return;
            var scheme = SchemeOf(device);
            if (scheme == Current)
                return;
            Current = scheme;
            Changed?.Invoke(scheme);
        }
    }
}
```

- [ ] **Step 4: Implementar `ControlHints`** (namespace de las pantallas, p. ej.
  `Zombineta.Juego.Screens`): `TextFor` devuelve la línea del esquema (Global Constraints,
  verbatim) + `"\n"` + la segunda línea. `OnEnable`: pinta `TextFor(InputDeviceTracker.Shared.Current)`
  y se suscribe a `Changed`; `OnDisable` se desuscribe. Solo usa `unscaled` si anima (no hace falta).

- [ ] **Step 5: Correr: PASS** (3 tests) y total en verde.

- [ ] **Step 6: Escena `Pause` por MCP**: un `Text` bajo los botones (mismo estilo que el resto,
  `verticalOverflow = Overflow`, trampa #25), con `ControlHints`. Guardar.

- [ ] **Step 7: Play por MCP**: abrir la pausa desde `Level_01` (`GameRoot.Flow.Pause()`), con joystick
  virtual apretar A → el texto de la pausa empieza con "Stick o cruceta"; con teclado virtual
  apretar una flecha → "W/S o ↑/↓". Captura del Game view (renderizando el Canvas overlay con
  `ScreenCapture.CaptureScreenshotAsTexture()` en el callback) y leerla.

- [ ] **Step 8: Commit**

```bash
git add Juego/Assets/_Zombineta/Scripts/Gameplay/Player/InputDeviceTracker.cs* Juego/Assets/_Zombineta/Scripts/Screens/ControlHints.cs* Juego/Assets/_Zombineta/Scenes/Pause.unity Juego/Assets/_Zombineta/Tests/Editor/InputDeviceTrackerTests.cs*
git commit -m "Ayuda de controles en la pausa segun teclado o joystick"
```

---

### Task 4: Recorrido con joystick y documentación

**Files:**
- Modify (solo si la verificación encuentra una pantalla con tecla suelta): `Z/Scripts/Screens/*.cs`
- Modify: `HANDOFF.md`

- [ ] **Step 1: Buscar teclas sueltas** — `grep -rn "Keyboard.current\|Mouse.current" Juego/Assets/_Zombineta/Scripts`:
  quedan solo `DebugHudView` y `LevelStub` (herramientas). Cualquier otra en pantallas → pasarla a
  acciones del mapa `UI`.

- [ ] **Step 2: Recorrido completo por MCP con joystick virtual** (callback antes de Play, desde
  `Boot`/`MainMenu`; navegación con `dpad`/`leftStick` y `buttonSouth`=Submit, `buttonEast`=Cancel):
  menú → Jugar → elegir personaje → saltear cinemática → `Level_01` (carril, turbo, disparo) →
  Start (pausa) → Opciones → interruptor Vibración (A lo cambia) → B vuelve → seguir → forzar
  derrota con Select+LB → Game Over → Reintentar con A → forzar victoria con Select+RB → Nivel
  completo → continuar con A. En cada paso registrar escenas cargadas y pantalla del flujo en
  `SessionState`. Consola sin errores.

- [ ] **Step 3: HANDOFF** — en la sección 0, subsección "Controles": tabla teclado/joystick, dónde
  se cambian (mapa `Moto` del asset de acciones), vibración (`Rumble.asset`, opción), ayuda en la
  pausa, F2/F3 con Select+RB/LB, límites (joysticks genéricos, vibración de PlayStation en
  Windows). Sección 6: verificado con joystick virtual; **no verificado**: joystick real (a probar
  por José). Trampas nuevas si aparecieron (p. ej. `InputTestFixture` requiere `testables`).

- [ ] **Step 4: Todos los tests en verde** (≈ 209) y commit.

```bash
git add HANDOFF.md Juego/Assets/_Zombineta/Scripts/Screens
git commit -m "docs: controles con joystick"
```
